import Lesmerpanel from 'nav-frontend-lesmerpanel';

import visibleIfHOC from '../../hocs/visible-if';

// eslint-disable-next-line import/prefer-default-export
export const VisibleIfLesmerpanel = visibleIfHOC(Lesmerpanel);
